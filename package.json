{
  "name": "launchdesk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LaunchDesk turns small service offers into launchable monthly revenue systems.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "smoke": "node scripts/smoke.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^5.2.1",
    "helmet": "^8.2.0",
    "morgan": "^1.10.1"
  }
}
